---
export interface Props {
  textColor?: string;
  className?: string;
}

const { textColor = "text-petroleum", className = "" } = Astro.props;
---

<div class={`flex items-center space-x-2 ${className}`}>
  <div class="w-8 h-8 gradient-bg rounded-lg flex items-center justify-center">
    <span class="text-white font-bold text-sm">4M</span>
  </div>
  <span class={`text-xl font-bold ${textColor}`}>4method</span>
</div>
